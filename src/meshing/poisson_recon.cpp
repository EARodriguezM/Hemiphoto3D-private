#include "meshing/poisson_recon.h"

// Stub — implemented in Step 9 (wraps PoissonRecon binary)
Mesh poissonReconstruct(const DensePointCloud& /*cloud*/,
                         int /*depth*/,
                         float /*screening*/) {
    return Mesh();
}
